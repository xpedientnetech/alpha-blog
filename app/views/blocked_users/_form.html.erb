<%= form_for(@blocked_user) do |f| %>
  <% if @blocked_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blocked_user.errors.count, "error") %> prohibited this blocked_user from being saved:</h2>

      <ul>
      <% @blocked_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :blocked_id %><br>
    <%= f.number_field :blocked_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
